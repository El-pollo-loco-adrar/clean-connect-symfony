<nav class="bg-[#F3F4F6] shadow-sm border-b-2 border-black relative">
    <div class="flex items-center justify-between max-w-screen-xl mx-auto px-4 py-4">

        {# Espaceur gauche #}
        <div class="flex-1 lg:block hidden"></div> {# Vide sur PC car on centre le logo #}
        <div class="flex-1 lg:hidden"></div>{# Vide sur Mobile #}

        {# LOGO (Toujours au milieu) #}
        <div class="flex-none">
            <a href="{{ path('app_home') }}" class="flex items-center">
                <img src="{{ asset('img/logo.png') }}" alt="Logo clean connect" class="h-20 lg:h-24">
            </a>
        </div>

        {# ZONE DROITE (Burger sur mobile, Déconnexion sur PC) #}
        <div class="flex-1 flex justify-end items-center">
            {% if app.user and (app.user.userType == 'employer' or 'candidate') %}
                {# BOUTON BURGER (Mobile uniquement) #}
                <button id="burger-btn" class="lg:hidden btn btn-ghost btn-square focus:outline-none">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="h-10 w-10 text-black">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
                    </svg>
                </button>

                {# Petit rappel utilisateur sur PC #}
                <div class="hidden lg:flex items-center gap-2 text-sm font-bold border-l-2 border-black pl-4">
                    <span>{{ app.user.firstname }}</span>
                    <a href="{{ path('app_logout') }}" class="text-red-600 hover:text-red-800">🚪Déconnexion</a>
                </div>
            
            {% else %}
                {# User non connecté #}
                <a href="#" title="Informations" class="text-gray-700 hover:text-blue-600 transition-colors">
                    <svg xmlns="http://www.w3.org/2000/svg" x="0px" y="0px" width="50" height="50" viewBox="0 0 50 50">
                    <path d="M 25 2 C 12.264481 2 2 12.264481 2 25 C 2 37.735519 12.264481 48 25 48 C 37.735519 48 48 37.735519 48 25 C 48 12.264481 37.735519 2 25 2 z M 25 4 C 36.664481 4 46 13.335519 46 25 C 46 36.664481 36.664481 46 25 46 C 13.335519 46 4 36.664481 4 25 C 4 13.335519 13.335519 4 25 4 z M 25 11 A 3 3 0 0 0 25 17 A 3 3 0 0 0 25 11 z M 21 21 L 21 23 L 23 23 L 23 36 L 21 36 L 21 38 L 29 38 L 29 36 L 27 36 L 27 21 L 21 21 z"></path>
                    </svg>
                </a>
            {% endif %}
        </div>
    </div>

    {# --- EMPLOYER : PARTIE BASSE : BARRE DE LIENS (PC UNIQUEMENT) --- #}
    {% if app.user and app.user.userType == 'employer' %}
        <div class="hidden lg:flex justify-center bg-[#F3F4F6] py-3">
            <ul class="flex gap-8 text-sm font-bold uppercase tracking-widest text-gray-700">
                <li><a href="{{ path('app_home') }}" class="hover:text-blue-600 transition-colors">Accueil</a></li>
                <li><a href="{{ path('app_profile_complete') }}" class="hover:text-blue-600 transition-colors">Mes informations</a></li>
                <li><a href="#" class="hover:text-blue-600 transition-colors">Documents</a></li>
                <li><a href="#" class="hover:text-blue-600 transition-colors">Historique des missions</a></li>
                <li><a href="#" class="hover:text-blue-600 transition-colors">Mes achats</a></li>
                <li><a href="#" class="hover:text-blue-600 transition-colors">Contact support</a></li>
            </ul>
        </div>
    {% endif %}

    {# --- CANDIDAT : PARTIE BASSE : BARRE DE LIENS (PC UNIQUEMENT) --- #}
    {% if app.user and app.user.userType == 'candidate' %}
        <div class="hidden lg:flex justify-center bg-white/50 py-3">
            <ul class="flex gap-8 text-sm font-bold uppercase tracking-widest text-gray-700">
                <li><a href="{{ path('app_home') }}" class="hover:text-blue-600 transition-colors">Accueil</a></li>
                <li><a href="{{ path('app_profile_complete') }}" class="hover:text-blue-600 transition-colors">Mes informations</a></li>
                <li><a href="{{ path('app_skills_candidate')}}" class="hover:text-blue-600 transition-colors">Mes compétences</a></li>
                <li><a href="#" class="hover:text-blue-600 transition-colors">Ma zone de déplacement</a></li>
                <li><a href="#" class="hover:text-blue-600 transition-colors">Mes disponibilités</a></li>
                <li><a href="#" class="hover:text-blue-600 transition-colors">Historique des missions</a></li>
                <li><a href="#" class="hover:text-blue-600 transition-colors">Contact support</a></li>
            </ul>
        </div>
    {% endif %}

    {# --- EMPLOYER : MENU MOBILE OVERLAY --- #}
    {% if app.user and app.user.userType == 'employer' %}
        <div id="burger-menu" class="hidden lg:hidden absolute top-full left-[10%] w-[80%] bg-[#F3F4F6] bg-opacity-98 z-[100] border-2 border-black rounded-lg shadow-2xl mt-2">
            <ul class="space-y-4 py-8 w-full text-xl font-bold text-black px-6 text-center">
                <div class="bg-gray-100 p-4 border-b border-black rounded-t-lg mb-4">
                    <p>{{ app.user.firstname }} {{ app.user.lastname }}</p>
                </div>
                <a href="{{ path('app_home')}}" class="block w-full"><li class="border-2 border-black bg-[#3B82F6]/20 rounded-lg p-4 shadow-xl">Accueil</li></a>
                <a href="#" class="block w-full"><li class="border-2 border-black bg-[#3B82F6]/20 rounded-lg p-4 shadow-xl">Mes informations</li></a>
                <a href="#" class="block w-full"><li class="border-2 border-black bg-[#3B82F6]/20 rounded-lg p-4 shadow-xl">Documents entreprise</li></a>
                <a href="#" class="block w-full"><li class="border-2 border-black bg-[#3B82F6]/20 rounded-lg p-4 shadow-xl">Historique des missions</li></a>
                <a href="#" class="block w-full"><li class="border-2 border-black bg-[#3B82F6]/20 rounded-lg p-4 shadow-xl">Contact support</li></a>

                <a href="{{ path('app_logout') }}" class="block w-full"><li class="p-4 bg-red-100 border-2 border-red-500 text-red-600 rounded-lg shadow-xl">Déconnexion</li></a>
            </ul>
        </div>
    {% endif %}

    {# --- CANDIDAT : MENU MOBILE OVERLAY --- #}
    {% if app.user and app.user.userType == 'candidate' %}
        <div id="burger-menu" class="hidden lg:hidden absolute top-full left-[10%] w-[80%] bg-[#F3F4F6] bg-opacity-98 z-[100] border-2 border-black rounded-lg shadow-2xl mt-2">
            <ul class="space-y-4 py-8 w-full text-xl font-bold text-black px-6 text-center">
                <div class="bg-gray-100 p-4 border-b border-black rounded-t-lg mb-4">
                    <p>{{ app.user.firstname }} {{ app.user.lastname }}</p>
                </div>
                <a href="{{ path('app_home')}}" class="block w-full"><li class="border-2 border-black bg-[#3B82F6]/20 rounded-lg p-4 shadow-xl">Accueil</li></a>
                <a href="{{ path('app_profile_complete') }}" class="block w-full"><li class="border-2 border-black bg-[#3B82F6]/20 rounded-lg p-4 shadow-xl">Mes informations</li></a>
                <a href="{{ path('app_skills_candidate')}}" class="block w-full"><li class="border-2 border-black bg-[#3B82F6]/20 rounded-lg p-4 shadow-xl">Mes compétences</li></a>
                <a href="#" class="block w-full"><li class="border-2 border-black bg-[#3B82F6]/20 rounded-lg p-4 shadow-xl">Ma zone de déplacement</li></a>
                <a href="#" class="block w-full"><li class="border-2 border-black bg-[#3B82F6]/20 rounded-lg p-4 shadow-xl">Mes disponibilités</li></a>
                <a href="#" class="block w-full"><li class="border-2 border-black bg-[#3B82F6]/20 rounded-lg p-4 shadow-xl">Historique des missions</li></a>
                <a href="#" class="block w-full"><li class="border-2 border-black bg-[#3B82F6]/20 rounded-lg p-4 shadow-xl">Contact support</li></a>

                <a href="{{ path('app_logout') }}" class="block w-full"><li class="p-4 bg-red-100 border-2 border-red-500 text-red-600 rounded-lg shadow-xl">Déconnexion</li></a>
            </ul>
        </div>
    {% endif %}
</nav>