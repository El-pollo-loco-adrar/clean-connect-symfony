{% use 'form_div_layout.html.twig' %}

{# 1. Style pour les champs TEXTE, DATE, etc. #}
{# {% block widget_attributes %}
    {% set attr = attr|merge({
        'class': (attr.class|default('') ~ ' w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500')|trim
    }) %}
    {{ parent() }}
{% endblock widget_attributes %} #}

{# 2. Style spécifique pour les DROPDOWNS (select) #}
{# {% block choice_widget_collapsed %}
    {% set attr = attr|merge({
        'class': (attr.class|default('') ~ ' w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 bg-white py-2 px-3')|trim
    }) %}
    {{ parent() }}
{% endblock choice_widget_collapsed %} #}

{# Style pour les Checkboxes #}
{% block checkbox_widget %}
    {% set attr = attr|merge({
        'class': (attr.class|default('') ~ ' rounded border-gray-300 text-indigo-600 shadow-sm focus:ring-indigo-500')|trim
    }) %}
    {{ parent() }}
{% endblock checkbox_widget %}

{# 3. Gestion des messages d'erreurs en rouge #}
{% block form_errors %}
    {% if errors|length > 0 %}
        <div class="mt-2 text-red-600">
            <ul class="list-none p-0 m-0 space-y-1">
                {% for error in errors %}
                    <li class="flex items-center gap-1.5 text-xs font-bold italic">
                        {# Petite icône d'alerte SVG #}
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 shrink-0" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
                        </svg>
                        {{ error.message }}
                    </li>
                {% endfor %}
            </ul>
        </div>
    {% endif %}
{% endblock form_errors %}